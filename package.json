{
  "name": "agentnet-stellar",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=@agentnet/web",
    "dev:backend": "turbo dev --filter=@agentnet/backend",
    "build": "turbo build",
    "build:backend": "turbo build --filter=@agentnet/backend",
    "build:web": "turbo build --filter=@agentnet/web",
    "build:sdk": "turbo build --filter=@agenticocean/*",
    "build:contracts": "cd contracts && cargo build --release --target wasm32v1-none",
    "test": "turbo test",
    "test:contracts": "cd contracts && cargo test --workspace",
    "test:unit": "vitest run --config tests/vitest.config.ts",
    "test:integration": "vitest run --config tests/vitest.integration.config.ts",
    "deploy:contracts": "pnpm --filter @agentnet/backend exec tsx src/scripts/deploy-contracts.ts",
    "deploy:contracts:mainnet": "pnpm --filter @agentnet/backend exec tsx src/scripts/deploy-contracts.ts --network mainnet",
    "setup:testnet": "pnpm --filter @agentnet/backend exec tsx src/scripts/setup-testnet.ts",
    "test:registry:testnet": "pnpm --filter @agentnet/backend exec tsx src/scripts/test-agent-registry.ts",
    "test:flow:testnet": "pnpm --filter @agentnet/backend exec tsx src/scripts/test-complete-flow.ts",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "vitest": "^2.1.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@10.29.2",
  "engines": {
    "node": ">=20.0.0"
  }
}
